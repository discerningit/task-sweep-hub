# Optional — only needed when wiring real AI APIs (future)
# VITE_GROK_API_KEY=
# VITE_ANTHROPIC_API_KEY=

# M365 Client ID is entered in the app Settings UI (not here).
# Register at https://portal.azure.com → App registrations