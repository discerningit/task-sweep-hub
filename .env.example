# Optional — local dev only (npm run dev). Prefer Settings → Grok API key on deployed app.
VITE_GROK_API_KEY=xai-your-key-here

# M365 Client ID is entered in the app Settings UI (not here).
# Register at https://portal.azure.com → App registrations